# WordPress source
WP_BASE_URL=https://www.btaskee.com

# Directus API
DIRECTUS_URL=http://192.168.88.85:8055
DIRECTUS_TOKEN=btaskee-static-token-1765362027-8eff48f89ca74ea5

# PostgreSQL - Main Directus Database (website-v3/cms docker)
PG_HOST=192.168.88.85
PG_PORT=5433
PG_USER=directus
PG_PASSWORD=directus
PG_DATABASE=directus

# PostgreSQL - Migration Tracking Database (separate, managed by docker-compose)
# These are set automatically in docker-compose.yml, only needed if running outside Docker
MIGRATION_PG_HOST=localhost
MIGRATION_PG_PORT=5434
MIGRATION_PG_USER=migration_user
MIGRATION_PG_PASSWORD=migration_pass
MIGRATION_PG_DATABASE=migration_tracking

# Migration settings
BATCH_SIZE=30
PARALLEL_LIMIT=6

# GUI Server
GUI_PORT=3001
